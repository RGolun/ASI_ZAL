<%= form_for(ball) do |f| %>
  <% if ball.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ball.errors.count, "error") %> prohibited this ball from being saved:</h2>

      <ul>
      <% ball.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
   <div class="col-sm-3 col-sm-offset-4">

 <div class="form-group">
  <div class="field">
    <%= f.label :brand %>
    <%= f.text_field :brand, autofocus: false, class: 'form-control' %>
  </div>
  </div>

  <div class="form-group">
  <div class="field">
    <%= f.label :size %>
    <%= f.number_field :size,  autofocus: false, class: 'form-control' %>
  </div>
  </div>

  <div class="form-group">
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description,  autofocus: false, class: 'form-control' %>
  </div>
  </div>

  <div class="form-group">
  <div class="actions">
    <%= f.submit class: "btn btn-primary btn-block btn-lg"%>

  </div>
  </div>

<% end %>
</div>
</div>
